version: '3.8'
services:
  db:
    image: postgres:17
    container_name: skill-exchange-db-container
    environment:
      POSTGRES_USER: skill_admin
      POSTGRES_PASSWORD: skill_password
      POSTGRES_DB: skill_exchange_db
    ports:
      - "5434:5432"